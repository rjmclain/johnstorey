// Combine all sagas to a single root saga.
import { fork } from "redux-saga/effects";

// Register all with a single generator.
export default function* startForeman() {
//  yield fork(exampleWatcher);
}